"""
l
l o
l o v
l o v e
"""

str= "love"
len_str = len(str)

for row in range(len_str):

    for col in range(row+1):
        print(str[col], end=" ")
        
    print()
    
    
    
    
    or
    
    

str = "love"
len = 4
new_str = ""

for row in range(1, len + 1):
    new_str += ( str[row-1] + " " )
    print(new_str)
